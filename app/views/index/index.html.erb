<h1>INDEX</h1>

<% @posts.each do |post| %>

<h2> <%= post.title %>  </h2>
<!--<p>  <%= post.content %> </p>  --> 
  <p>
    <a href="<%= index_path(post.id)%>" class="btn btn-primary"> Lire cet article</a>
    <a href="<%= index_path(post.id)%>" class="btn btn-danger" data-confirm="êtes vous sûr de supprimer ?" data-method="DELETE"> Supprimer</a>
  </p>


<% end %>
  <p>
    <a href="<%= new_index_path%>" class="btn btn-success"> créer un article </a>
  </p>
